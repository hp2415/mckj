[Network]
# 后端 API 地址 (本地回环使用 http://localhost:8000)
api_url = http://192.168.0.125:8000
# 网络超时时间 (秒)
timeout = 15

[Runtime]
# 日志级别: DEBUG, INFO, WARNING, ERROR
log_level = INFO
# 云端数据自动同步间隔 (分钟)
sync_interval_min = 10
